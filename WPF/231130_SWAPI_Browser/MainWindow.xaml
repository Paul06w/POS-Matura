<Window x:Class="_231130_SWAPI_Browser.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_231130_SWAPI_Browser"
        mc:Ignorable="d"
        Background="Gray"
        Title="Star Wars People" Height="650" Width="1100" Icon="/Darth_Vader.png">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="0.5*"/>
            <RowDefinition Height="6*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Image Grid.Row="0" Grid.ColumnSpan="2" x:Name="imageStarWars" Source="/StarWars.png"/>
        <Label Grid.Row="1" Grid.ColumnSpan="2" x:Name="labelinfo" Content="Personen werden geladen..." VerticalContentAlignment="Center" HorizontalContentAlignment="Left" Foreground="Red" FontSize="18" Margin="20,0,0,0"/>
        <DataGrid Grid.Row="2" Grid.Column="0" x:Name="dataGridPeopleList" Margin="20,20,20,20" MouseDoubleClick="dataGridPeopleList_MouseDoubleClick" IsReadOnly="True" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}"></DataGridTextColumn>
                <DataGridTextColumn Header="Birthyear" Binding="{Binding Birthyear}"></DataGridTextColumn>
                <DataGridTextColumn Header="Gender" Binding="{Binding Gender}"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
        <DataGrid Grid.Row="2" Grid.Column="1" x:Name="dataGridStarshipList" Margin="20,20,20,20" Visibility="Hidden" IsReadOnly="True" />
        
        
    </Grid>
</Window>
